CREATE TABLE 
			covid_dataset (
			province VARCHAR(150),
			country_or_region VARCHAR(200),
			latitude FLOAT,
			longitude FLOAT,
			date DATE,
			confirmed INT,
			deaths INT,
			recovered INT
);

